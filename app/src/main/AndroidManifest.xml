<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gov.guia.guiacomumdorecife">

    <application
        android:name=".GuiaComumApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.AberturaActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.MenuInicialActivity" />
        <activity android:name=".view.guia.MapaActivity" />
        <activity android:name=".view.guia.LivrosActivity" />
        <activity android:name=".view.guia.ConteudoActivity" />
        <activity android:name=".view.guia.GaleriaActivity"></activity>
    </application>

</manifest>